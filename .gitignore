# User config — never overwrite on git pull
# config.example.yaml is the tracked template
config.yaml

# ESPHome secrets
esphome/secrets.yaml

# ESPHome generated configs (produced by install script, not committed)
esphome/generated/

# Python
__pycache__/
*.pyc
*.pyo
.env
venv/

# MCP server config (local dev tooling)
.mcp.json

# mex scaffold (local AI context, not tracked)
.mex/

# Ollama suggestions (local dev notes)
ollama_suggestions.md

# OS
.DS_Store
Thumbs.db
README.backup.md
ENHANCEMENTS.md
middleware/docs/superpowers/
.cocoindex_code/
.ragcode/
